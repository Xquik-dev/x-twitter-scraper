# Repository metadata check

Checked on 2026-08-22.

The GitHub description is:

> X (Twitter) Scraper API and X API Alternative. You do not need an official X
> developer account. You do not need to connect or use an X account for
> supported scraping. Not affiliated with X Corp.

The homepage is `https://xquik.com`. The repository has 20 topics, including
`twitter-scraper-api`, `twitter-scraper`, `x-api-alternative`,
`twitter-api-alternative`, `mcp`, `sdk`, `tweet-search`, `data-extraction`, and
`webhooks`.

`tests/metadata-positioning.test.mjs` checks the product phrases and account
boundary across package, plugin, marketplace, registry, Skill, Context7,
OpenClaw, and Docker metadata. It also checks the short MCP registry count.

`node scripts/check-versions.mjs` confirms bundle v2.6.7 and hosted MCP v2.6.0
stay separate.
