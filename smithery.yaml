startCommand:
  type: http
  configSchema:
    type: object
    required:
      - apiKey
    properties:
      apiKey:
        type: string
        description: "Xquik API key from https://dashboard.xquik.com/account"
  url: "https://xquik.com/mcp"
